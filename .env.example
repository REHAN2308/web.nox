# ===========================================
# Web.nox Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# ===========================================
# SMTP / Email Configuration (Required for contact form)
# ===========================================

# SMTP server host (e.g., smtp.gmail.com, smtp.mailgun.org)
SMTP_HOST=smtp.gmail.com

# SMTP port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP username (your email address for most providers)
SMTP_USER=your-email@gmail.com

# SMTP password or App Password
# For Gmail: Use an App Password (https://support.google.com/accounts/answer/185833)
# For other providers: Use your SMTP password or API key
SMTP_PASS=your-app-password-here

# Email address to receive contact form submissions
CONTACT_TO_EMAIL=your-email@gmail.com

# ===========================================
# Optional Configuration
# ===========================================

# Site URL (for production)
# NEXT_PUBLIC_SITE_URL=https://webnox.com

# Analytics (if using)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
